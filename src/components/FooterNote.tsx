export function FooterNote() {
  return (
    <footer className="note">
      <span>
        <strong>Hover the text field</strong> to reveal the interactive mask region.
      </span>
      <span>Press Enter to regenerate the composition.</span>
    </footer>
  );
}
